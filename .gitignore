# Build outputs
dist/
dist-store/
node_modules/

# Logs
*.log

# Extension keypair (NEVER commit private key)
.extension-key.pem

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local environment
.env
.env.local

# Bridge runtime artifacts
bridge.log

# Cached absolute node path used by launcher.sh (see bridge/launcher.sh) —
# host-specific, regenerated by install.sh/install.ps1 on every install.
bridge/.node-path

# playwright-cli scratch (defensive — should now go to %LOCALAPPDATA%\Anya\playwright)
bridge/.playwright-cli/
